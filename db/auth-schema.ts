import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
const date=(name:string)=>integer(name,{mode:'timestamp_ms'});
export const user=sqliteTable('auth_users',{id:text('id').primaryKey(),name:text('name').notNull(),email:text('email').notNull().unique(),emailVerified:integer('email_verified',{mode:'boolean'}).notNull().default(false),image:text('image'),createdAt:date('created_at').notNull(),updatedAt:date('updated_at').notNull()});
export const session=sqliteTable('auth_sessions',{id:text('id').primaryKey(),expiresAt:date('expires_at').notNull(),token:text('token').notNull().unique(),createdAt:date('created_at').notNull(),updatedAt:date('updated_at').notNull(),ipAddress:text('ip_address'),userAgent:text('user_agent'),userId:text('user_id').notNull().references(()=>user.id,{onDelete:'cascade'})},t=>[index('auth_session_user').on(t.userId)]);
export const account=sqliteTable('auth_accounts',{id:text('id').primaryKey(),accountId:text('account_id').notNull(),providerId:text('provider_id').notNull(),userId:text('user_id').notNull().references(()=>user.id,{onDelete:'cascade'}),accessToken:text('access_token'),refreshToken:text('refresh_token'),idToken:text('id_token'),accessTokenExpiresAt:date('access_token_expires_at'),refreshTokenExpiresAt:date('refresh_token_expires_at'),scope:text('scope'),password:text('password'),createdAt:date('created_at').notNull(),updatedAt:date('updated_at').notNull()},t=>[index('auth_account_user').on(t.userId),uniqueIndex('auth_provider_account').on(t.providerId,t.accountId)]);
export const verification=sqliteTable('auth_verifications',{id:text('id').primaryKey(),identifier:text('identifier').notNull(),value:text('value').notNull(),expiresAt:date('expires_at').notNull(),createdAt:date('created_at').notNull(),updatedAt:date('updated_at').notNull()},t=>[index('auth_verification_identifier').on(t.identifier)]);
export const rateLimit=sqliteTable('auth_rate_limits',{id:text('id').primaryKey(),key:text('key').notNull().unique(),count:integer('count').notNull(),lastRequest:integer('last_request').notNull()});
