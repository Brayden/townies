import assert from 'node:assert/strict';import {build} from 'esbuild';import {readFile} from 'node:fs/promises';import {createServer} from 'node:http';
const {chromium}=await import(process.env.PLAYWRIGHT_MODULE??'/Users/brayden/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs');
const bundle=await build({stdin:{contents:`import React,{useState} from 'react';import {createRoot} from 'react-dom/client';import TownChatButton from './app/game/TownChatButton';import TownHeader from './app/game/TownHeader';function Test(){const [open,setOpen]=useState(false);return <main className="game"><TownHeader data={null} active={false} onElection={()=>{}} onTown={()=>{}} onSettings={()=>{}}/><nav className={'hud bottom-dock '+(location.hash==='#job'?'job-dock':'')}>{['Home','Work','Journal','Market','Town'].map(t=><button key={t} className="dock-button">{t}</button>)}</nav><div className="map-hud"/><TownChatButton unread={12} open={open} onClick={()=>setOpen(v=>!v)}/>{!open&&<div className="mobile-controls"><div className="joystick"/></div>}{open&&<section id="town-chat" className="hud town-chat paper"><div className="chat-messages">Town conversation</div><input aria-label="Message"/></section>}</main>}createRoot(document.getElementById('root')).render(<Test/>);`,loader:'tsx',resolveDir:process.cwd()},bundle:true,write:false,format:'iife',define:{'process.env.NODE_ENV':'"production"'}});
const css=(await readFile('app/globals.css','utf8')).split('\n').slice(5).join('\n');const server=createServer((req,res)=>res.end(`<!doctype html><meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"><style>${css}</style><div id="root"></div><script>${bundle.outputFiles[0].text}</script>`));await new Promise(r=>server.listen(0,'127.0.0.1',r));const browser=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});
try{const page=await browser.newPage();for(const size of [{width:390,height:844},{width:320,height:568},{width:844,height:390},{width:1440,height:900}])for(const job of [false,true]){
 await page.setViewportSize(size);await page.goto(`http://127.0.0.1:${server.address().port}/${job?'#job':''}`);const button=page.getByRole('button',{name:'Town chat, 12 unread messages',exact:true});await button.waitFor();const b=await button.boundingBox(),dock=await page.locator('.bottom-dock').boundingBox();assert.ok(b.x<25&&b.y>size.height/2);assert.ok(b.y+b.height<=dock.y,'Chat button clears the dock');
 const joystick=page.locator('.mobile-controls');if(await joystick.isVisible()){const j=await joystick.boundingBox();assert.ok(j.y+j.height<=b.y,'Joystick clears chat button')}
 await button.click();assert.equal(await button.getAttribute('aria-expanded'),'true');const panel=await page.locator('#town-chat').boundingBox();assert.ok(panel.x>=0&&panel.x+panel.width<=size.width);assert.ok(panel.y>=0&&panel.y+panel.height<=b.y,'Conversation appears above its button');await button.click();assert.equal(await page.locator('#town-chat').count(),0);
 }console.log('PASS: lower-left chat, unread badge, open/close, and no dock/joystick overlap across phone, landscape, desktop and job layouts.');}finally{await browser.close();await new Promise(r=>server.close(r))}
